rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write for all users (TEST MODE - for development only)
      // For production, you should set proper authentication rules
      allow read, write: if true;
    }
  }
}
